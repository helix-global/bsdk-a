CREATE TABLE [dbo].[SCode] (
    [SCode]        VARCHAR (8) NOT NULL,
    [SCodeName]    NTEXT       NOT NULL,
    [SCodeMessage] NTEXT       NOT NULL,
    CONSTRAINT [SCode_PK] PRIMARY KEY CLUSTERED ([SCode] ASC)
);




